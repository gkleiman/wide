# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Project:
  fields:
    default: name, user, project_type
    form: name, user, project_type
  order_by: user_id
  relationships: project_type, repository, user
  filters: user
  search: name
  application: wide

ProjectType:
  fields:
    default: name, description
    form: name, description, makefile_template
  order_by:
  relationships:
  filters:
  search: name
  application: wide

Repository:
  fields:
    default: project, path, scm
    form: project, path, url, scm
  order_by:
  relationships: project
  filters:
  search:
  application: wide

SshKey:
  fields:
    default: user, name_base64_encoded, name, content
    form: user, name_base64_encoded, name, content
  order_by:
  relationships: user
  filters:
  search: name
  application: wide

User:
  fields:
    default: user_name, active, email
    form: user_name, active, email
  order_by:
  relationships: projects, ssh_keys
  filters: active
  search: user_name, email
  application: wide

